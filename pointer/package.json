{
  "name": "falai",
  "version": "3.0.0",
  "description": "Alias for @falai/agent — re-exports it unchanged. Install @falai/agent directly.",
  "type": "module",
  "main": "./index.cjs",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "import": "./index.js",
      "require": "./index.cjs"
    }
  },
  "files": [
    "index.js",
    "index.cjs",
    "index.d.ts",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/falai-dev/agent.git",
    "directory": "pointer"
  },
  "bugs": {
    "url": "https://github.com/falai-dev/agent/issues"
  },
  "homepage": "https://falai.dev",
  "scripts": {
    "check": "node --input-type=module -e \"import('falai').then(m => { if (!m.createAgent) throw new Error('ESM alias: createAgent missing'); })\" && node -e \"if (!require('falai').createAgent) throw new Error('CJS alias: createAgent missing')\"",
    "prepublishOnly": "bun run check",
    "release": "bun publish"
  },
  "author": "Gustavo Salomé <gusnips>",
  "license": "MIT",
  "engines": {
    "node": ">=22.12"
  },
  "dependencies": {
    "@falai/agent": "^3.0.0"
  }
}
